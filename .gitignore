.DS_Store
node_modules/
dist/

# Shared Cargo target dir for both apps (see .cargo/config.toml).
/target/

.tmp/

# Throwaway dev database built from migrations for entity codegen (dev loop).
/dev.db
/dev.db-*

# Icon-subsetting venv + generated per-app subset fonts (built by
# scripts/subset-icons.py via each app's predev/prebuild hook).
/scripts/.venv/
/main/src/assets/fonts/material-symbols-subset.woff2
/tray/src/assets/fonts/material-symbols-subset.woff2
